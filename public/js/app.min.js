/**
 * 初始化
 */
var app = angular.module('screen', ['ui.router']).run(function() {
    require.config({
        paths: {
            echarts: './vendor/echarts'
        },
        packages: [{
            name: 'zrender',
            location: './vendor/zrender',
            main: 'zrender'
        }]
    });
});

app.run(['$rootScope', 'timer', function($rootScope, timer) {
    $rootScope.$on('$locationChangeStart', function() {
        timer.killAll();
    });
}]);
/**
 * 全局参数配置
 */
app.constant('M_API', {
     base: '/',  //工程地址
     speechList:'speechList',//言论趋势(近30天)
     speechDate:'speechDate',//言论趋势(今日昨日)
     centralizedRadar:'centralizedRadar',//归口占比
     placeList: 'placeList', //舆论场列表
     placeDetail: 'placeDetail', //舆论场24小时详细
     wordsList: 'wordsList', //涉容热词
     getHotPeopleListPath:'getHotPeopleList',//获取热点人物
     getHotNewListPath:'getHotNewList',//获取热点人物对应的新闻话题
     webSum: 'webSum'    //总抓取数量统计
});


//app.constant('M_API', {
//    base: '/LargeScreen/',  //工程地址
//    speechList:'count/getSpeechTrends.do',//言论趋势(近30天)
//    speechDate:'count/getDaySpeechTrends.do',//言论趋势(今日昨日)
//    centralizedRadar:'count/getPercent.do',//归口占比
//    placeList: 'count/getCount.do', //舆论场列表
//    placeDetail: 'count/getHour.do', //舆论场24小时详细
//    wordsList: 'count/getKeywords.do', //涉容热词
//    getHotPeopleListPath:'count/getPeople.do',//获取热点人物
//    getHotNewListPath:'count/getNew.do',//获取热点人物对应的新闻话题
//    webSum: 'count/getAll.do'    //总抓取数量统计
//});



//http服务配置
app.config(['$httpProvider', function($httpProvider) {
    // 修改传值格式为form
    $httpProvider.defaults.headers.post['Content-Type'] = 'application/x-www-form-urlencoded;charset=utf-8';

    /**
     * 参数序列化
     */
    var param = function(obj) {
        var query = '', name, value, fullSubName, subName, subValue, innerObj, i;

        for(name in obj) {
            value = obj[name];

            if(value instanceof Array) {
                for(i=0; i<value.length; ++i) {
                    subValue = value[i];
                    fullSubName = name + '[' + i + ']';
                    innerObj = {};
                    innerObj[fullSubName] = subValue;
                    query += param(innerObj) + '&';
                }
            }
            else if(value instanceof Object) {
                for(subName in value) {
                    subValue = value[subName];
                    fullSubName = name + '[' + subName + ']';
                    innerObj = {};
                    innerObj[fullSubName] = subValue;
                    query += param(innerObj) + '&';
                }
            }
            else if(value !== undefined && value !== null)
                query += encodeURIComponent(name) + '=' + encodeURIComponent(value) + '&';
        }

        return query.length ? query.substr(0, query.length - 1) : query;
    };

    // 设置自动序列化
    $httpProvider.defaults.transformRequest = function(data) {
        return angular.isObject(data) && String(data) !== '[object File]' ? param(data) : data;
    };
}]);
app.config(['$stateProvider', '$urlRouterProvider', 'M_API',
    function($stateProvider, $urlRouterProvider, M_API) {
        $urlRouterProvider.otherwise('/l/place');

        $stateProvider
            .state('l', {
                url: '/l',
                views: {
                    '': {
                        templateUrl: M_API.base + 'tpls/layout.html',
                        controller: "initWebCtrl"
                    }
                },
                abstract: true
            })
            //舆论场
            .state('l.place', {
                url: '/place',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/place.html',
                        controller: "placeCtrl"
                    }
                }
            })
            //热点人物
            .state('l.figure', {
                url: '/figure',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/figure.html',
                        controller: "figureCtrl"
                    }
                }
            })
            //涉容热词
            .state('l.words', {
                url: '/words',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/words.html',
                        controller: "wordsCtrl"
                    }
                }
            })
            //言论趋势
            .state('l.speech', {
                url: '/speech',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/speech.html',
                        controller: "speechCtrl"
                    }
                }
            })
            //归口占比
            .state('l.centralized', {
                url: '/centralized',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/centralized.html',
                        controller: "centralizedCtrl"
                    }
                }
            })
            //事件传播
            .state('l.event', {
                url: '/event',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/event.html',
                        controller: "eventCtrl"
                    }
                }
            })
            //数据抓取
            .state('l.grab', {
                url: '/grab',
                views: {
                    'content': {
                        templateUrl: M_API.base + 'tpls/grab.html',
                        controller: "grabCtrl"
                    }
                }
            });
    }
]);